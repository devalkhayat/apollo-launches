
query GetLaunches($pageSize: Int, $after: String) {
    launches(pageSize: $pageSize, after: $after) {
        launches {
            id
            mission {
                name
                missionPatch(size: SMALL) # <-- add this line
            }
            rocket {
                name
            }
        }
        cursor
        hasMore
    }
}